# PostgreSQL → Slack Connector

Generated by **Aivar Discovery Agent — Level 3**.

## Authentication

**Type:** `basic`

PostgreSQL uses basic authentication with database credentials through the psycopg2 driver. The credentials are typically the database username and password.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Provide your API token via environment variable or pass directly:
   ```bash
   export API_TOKEN="your-token-here"
   ```

## Usage

```python
from connector import PostgresqlToSlackConnector

client = PostgresqlToSlackConnector(api_token="your-token-here")

# List all shipment records
records = client.list_records()

# Get a single record by ID
record = client.get_record("record-id")

# Create a new record
new_record = client.create_record({"field": "value"})

# Update an existing record
updated = client.update_record("record-id", {"field": "new-value"})
```

## Running Tests

```bash
pytest test_connector.py -v
```

Tests use mocked HTTP — no real API credentials required.

## Files

| File | Description |
|------|-------------|
| `connector.py` | Integration connector (auth, CRUD, pagination, retries) |
| `agent_def.yaml` | Agent definition (system prompt, tools, workflow, test scenarios) |
| `test_connector.py` | Unit tests with mocked HTTP |
| `requirements.txt` | Python dependencies |
| `README.md` | This file |
