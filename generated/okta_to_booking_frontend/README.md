# Okta → Booking Frontend Connector

Generated by **Aivar Discovery Agent — Level 3**.

## ⚠️ Verify before deploying

The following values were inferred from general API knowledge of **Booking Frontend**,
not from your source documents. Confirm each against the live API documentation before use:

- **`api_base_url`**: `https://{org}.okta.com/api/v1`
- **`pagination_cursor_field`**: `next_cursor`
- **`auth_type`**: `bearer`
- **`list_response_key`**: ``

> These values compile and pass the mocked unit tests, but may need adjustment for
> your specific Booking Frontend instance, tenant URL, or API version.

## Authentication

**Type:** `bearer`

To obtain credentials, go to Okta's dashboard and navigate to Security > API > Tokens. Click on 'Create Token' to generate a new API token.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Provide your API token via environment variable or pass directly:
   ```bash
   export API_TOKEN="your-token-here"
   ```

## Usage

```python
from connector import OktaToBookingFrontendConnector

client = OktaToBookingFrontendConnector(api_token="your-token-here")

# List all staff records
records = client.list_staffs()

# Get a single record by ID
record = client.get_staff("record-id")

# Create a new record
new_record = client.create_staff({"field": "value"})

# Update an existing record
updated = client.update_staff("record-id", {"field": "new-value"})
```

## Running Tests

```bash
pytest test_connector.py -v
```

Tests use mocked HTTP — no real API credentials required.

## Files

| File | Description |
|------|-------------|
| `connector.py` | Integration connector (auth, CRUD, pagination, retries) |
| `agent_def.yaml` | Agent definition (system prompt, tools, workflow, test scenarios) |
| `test_connector.py` | Unit tests with mocked HTTP |
| `requirements.txt` | Python dependencies |
| `README.md` | This file |
