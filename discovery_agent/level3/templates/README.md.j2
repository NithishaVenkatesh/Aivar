# {{ source_system }} → {{ destination_system }} Connector

Generated by **Aivar Discovery Agent — Level 3**.
{% if inferred_fields %}

## ⚠️ Verify before deploying

The following values were inferred from general API knowledge of **{{ destination_system }}**,
not from your source documents. Confirm each against the live API documentation before use:

{% for field in inferred_fields %}
- **`{{ field }}`**: `{{ inferred_values[field] }}`
{% endfor %}

> These values compile and pass the mocked unit tests, but may need adjustment for
> your specific {{ destination_system }} instance, tenant URL, or API version.
{% endif %}

## Authentication

**Type:** `{{ auth_type }}`

{{ auth_notes }}

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Provide your API token via environment variable or pass directly:
   ```bash
   export API_TOKEN="your-token-here"
   ```

## Usage

```python
from connector import {{ class_name }}

client = {{ class_name }}(api_token="your-token-here")

# List all {{ entity_name }} records
records = client.list_{{ entity_name }}s()

# Get a single record by ID
record = client.get_{{ entity_name }}("record-id")

# Create a new record
new_record = client.create_{{ entity_name }}({"field": "value"})

# Update an existing record
updated = client.update_{{ entity_name }}("record-id", {"field": "new-value"})
```

## Running Tests

```bash
pytest test_connector.py -v
```

Tests use mocked HTTP — no real API credentials required.

## Files

| File | Description |
|------|-------------|
| `connector.py` | Integration connector (auth, CRUD, pagination, retries) |
| `agent_def.yaml` | Agent definition (system prompt, tools, workflow, test scenarios) |
| `test_connector.py` | Unit tests with mocked HTTP |
| `requirements.txt` | Python dependencies |
| `README.md` | This file |
